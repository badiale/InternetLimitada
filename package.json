{
  "name": "InternetLimitada",
  "version": "1.0.0",
  "description": "Simule sua internet limitada hoje mesmo!",
  "main": "src/index.js",
  "scripts": {
    "lint": "standard src/**/*.js builder/**/*.js",
    "clean": "rimraf dist",
    "build-chrome": "node builder/chrome.js",
    "build": "npm run lint && npm run clean && npm run build-chrome",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.0.0",
    "cpr": "^1.1.1",
    "mkdirp": "^0.5.1",
    "pn": "^1.0.0",
    "q": "^1.4.1",
    "rimraf": "^2.5.2",
    "standard": "^6.0.8",
    "svg2png": "^3.0.0"
  },
  "standard": {
    "env": [
      "node",
      "browser"
    ]
  },
  "dependencies": {
    "bytes": "^2.3.0"
  }
}
